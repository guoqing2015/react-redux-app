import styled from 'styled-components';

const Wrapper = styled.div`
  font-size: 0.7rem;
  text-align: center;
  position: relative;
  z-index: 1000;
  background: #ffffff;
    
  &.current {
   color: #249ace;
  }
  
 
`;

export default Wrapper;
