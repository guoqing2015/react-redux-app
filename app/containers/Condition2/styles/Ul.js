import styled from 'styled-components';

const Ul = styled.ul`
    // position: fixed;
    // top:  2rem;
    // left: 0;
    // right: 0;
    // z-index: 999;
     // overflow-y: scroll;
     // -webkit-overflow-scrolling: touch;
`;

export default Ul;
