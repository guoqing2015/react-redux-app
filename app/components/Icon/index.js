import React from 'react';
import PropTypes from 'prop-types';
import StyleIcon from './styles/StyleIcon';

function Icon(props) {
  return (
	  <StyleIcon {...props} />
  );
}

Icon.propTypes = {
};


export default Icon;
